# Release process

This repository keeps source, tests, CLI smoke, package preview, and public README surfaces aligned before any package release proposal. A package release should only happen after an independent fresh-clone audit confirms build, tests, smoke commands, package contents, README surfaces, and public-safety greps.

This document describes product release hygiene only. It is product release guidance and does not authorize publishing, tagging, or history changes.
