# Package Publishing Notes

This repo prepares `@machinefi/runtime` for package audit. Do not publish from CI. Run `npm pack --dry-run --json` and inspect the file whitelist before any release approval.
