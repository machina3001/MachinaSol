# Public Claims

Supported claims in this checkout: SDK/CLI interfaces, Solana receipt verification, wallet-linked unsigned settlement intents, fixture CI mode, caller-supplied CLI/SDK Solana provider configuration, operator-controlled HTTP-server provider configuration, and the package name `@machinefi/runtime`.

Robinhood/EVM support is not a supported claim: no Robinhood adapter exists in this source tree.

Launch copy should state what developers can install, run, and verify with this package: typed sessions, adapter-backed receipt checks, settlement intent builders, and fixture-mode CLI smoke commands.
